# Tensorflow 2 & Keras

0. Sequential: a function that builds a neural network with the Keras library.

1. Input: a function that builds a neural network with the Keras library.

2. Optimize: a function that sets up Adam optimization for a keras model.

3. One Hot: a function that converts a label vector into a one-hot matrix.

4. Train: a function that trains a model using mini-batch gradient descent.

5. Validate: update the function train_model to also analyze validaiton data.

6. Early Stopping: update the function train_model to using early stopping.

7. Learning Rate Decay: update to train the model with learning rate decay.

8. Save Only the Best: update to also save the best iteration of the model.

9. Save and Load Model: a functions saves and load an entire model.

10. Save and Load Weights: a functions saves and loads a model's weights.

11. Save and Load Configuration: saves and loads configuration in JSON format.

12. Test: a function that tests a neural network.

13. Predict: a function that makes a prediction using a neural network.
