# Optimization

0. Normalization Constants: a function that calculates the normalization const

1. Normalize: a function that normalizes (standardizes) a matrix.

2. Shuffle Data: a function that shuffles the data points in two matrices.

3. Mini-Batch: a function that creates mini-batches using gradient descent.

4. Moving Average: a function calculate weighted moving average.

5. Momentum: a function that update variables using gradient descent.

6. Momentum Upgraded: a function create a momentum optimizer in TensorFlow.

7. RMSProp: a function to update variables using the RMSProp optimization.

8. RMSProp Upgraded: a function to create an RMSProp optimizer in TensorFlow.

9. Adam: a function update variables using the Adam optimization algorithm.

10. Adam Upgraded: a function create an Adam optimizer in TensorFlow.

11. Learning Rate Decay: a function update learning rate using inverse time decay.

12. Learning Rate Decay Upgraded: a function create a learning rate decay operation in TensorFlow.

13. Batch Normalization: a function create batch normalization layer.

14. Batch Normalization Upgraded: a function create batch normalization layer.
